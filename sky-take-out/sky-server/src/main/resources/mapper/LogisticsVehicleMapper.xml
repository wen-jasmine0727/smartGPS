<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sky.logistics.mapper.LogisticsVehicleMapper">

    <sql id="VehicleColumns">
        id,
        plate,
        vin_topic AS vinTopic,
        vehicle_type AS vehicleType,
        capacity,
        driver_name AS driverName,
        driver_phone AS driverPhone,
        device_imei AS deviceImei,
        status,
        device_status AS deviceStatus,
        created_at AS createdAt,
        updated_at AS updatedAt
    </sql>

    <sql id="VehicleWhere">
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                    plate ILIKE CONCAT('%', #{keyword}, '%')
                    OR driver_name ILIKE CONCAT('%', #{keyword}, '%')
                    OR device_imei ILIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
    </sql>

    <select id="findPage" resultType="com.sky.logistics.entity.Vehicle">
        SELECT
        <include refid="VehicleColumns"/>
        FROM vehicles
        <include refid="VehicleWhere"/>
        ORDER BY updated_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM vehicles
        <include refid="VehicleWhere"/>
    </select>

    <select id="findByPlate" resultType="com.sky.logistics.entity.Vehicle">
        SELECT
        <include refid="VehicleColumns"/>
        FROM vehicles
        WHERE plate = #{plate}
    </select>

    <select id="findById" resultType="com.sky.logistics.entity.Vehicle">
        SELECT
        <include refid="VehicleColumns"/>
        FROM vehicles
        WHERE id = #{id}
    </select>

    <select id="findByPlateExcludeId" resultType="com.sky.logistics.entity.Vehicle">
        SELECT
        <include refid="VehicleColumns"/>
        FROM vehicles
        WHERE plate = #{plate}
          AND id &lt;&gt; #{id}
    </select>

    <select id="findByDeviceImei" resultType="com.sky.logistics.entity.Vehicle">
        SELECT
        <include refid="VehicleColumns"/>
        FROM vehicles
        WHERE device_imei = #{deviceImei}
    </select>

    <select id="findByDeviceImeiExcludeId" resultType="com.sky.logistics.entity.Vehicle">
        SELECT
        <include refid="VehicleColumns"/>
        FROM vehicles
        WHERE device_imei = #{deviceImei}
          AND id &lt;&gt; #{id}
    </select>

    <select id="countBindingById" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM cargo_vehicle_binding b
        JOIN vehicles v ON v.plate = b.plate
        WHERE v.id = #{id}
    </select>

    <insert id="insert"
            parameterType="com.sky.logistics.entity.Vehicle"
            useGeneratedKeys="true"
            keyProperty="id"
            keyColumn="id">
        INSERT INTO vehicles (
            plate,
            vin_topic,
            vehicle_type,
            capacity,
            driver_name,
            driver_phone,
            device_imei,
            status,
            device_status,
            created_at,
            updated_at
        ) VALUES (
            #{plate},
            #{vinTopic},
            #{vehicleType},
            #{capacity},
            #{driverName},
            #{driverPhone},
            #{deviceImei},
            #{status},
            #{deviceStatus},
            now(),
            now()
        )
    </insert>

    <update id="update" parameterType="com.sky.logistics.entity.Vehicle">
        UPDATE vehicles
        SET
            plate = #{plate},
            vin_topic = #{vinTopic},
            vehicle_type = #{vehicleType},
            capacity = #{capacity},
            driver_name = #{driverName},
            driver_phone = #{driverPhone},
            device_imei = #{deviceImei},
            status = #{status},
            device_status = #{deviceStatus},
            updated_at = now()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM vehicles
        WHERE id = #{id}
    </delete>

</mapper>
